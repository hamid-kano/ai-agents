# مدونة تقنية بالوكلاء الأذكياء

نظام متكامل لإدارة مدونة تقنية باستخدام Laravel + Next.js + DeepSeek AI

## المميزات

- 🤖 **4 وكلاء ذكيين**: باحث، كاتب، محرر، خبير SEO
- 🚀 **DeepSeek V3**: نموذج قوي واقتصادي
- 📝 **إنشاء مقالات تلقائي**: من الفكرة إلى المقال الجاهز
- 🎨 **واجهة Next.js**: سريعة وحديثة

## التثبيت

### Backend (Laravel)

```bash
cd ai-agents

# 1. تثبيت المكتبات
composer install

# 2. إعداد البيئة
copy .env.example .env
php artisan key:generate

# 3. إضافة مفتاح DeepSeek في .env
DEEPSEEK_API_KEY=your_key_here
DB_DATABASE=ai_agents

# 4. إنشاء قاعدة البيانات
php artisan migrate

# 5. تشغيل السيرفر
php artisan serve
```

### Frontend (Next.js)

```bash
cd ai-agents-frontend

# 1. تثبيت المكتبات
npm install

# 2. تشغيل السيرفر
npm run dev
```

## الاستخدام

1. افتح المتصفح: `http://localhost:3000`
2. اضغط "مقال جديد"
3. أدخل موضوع المقال
4. انتظر الوكلاء ينجزون العمل!

## الوكلاء

- **ResearcherAgent**: يبحث عن الموضوع ويجمع المعلومات
- **WriterAgent**: يكتب مقال شامل بناءً على البحث
- **EditorAgent**: يراجع ويحسن الصياغة
- **SEOAgent**: يحسن المقال لمحركات البحث

## التكلفة

DeepSeek أرخص 90% من GPT-4:
- 1000 مقال ≈ $5-10 فقط!
